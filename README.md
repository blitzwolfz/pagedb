# PageDB

Small key-value database that stores data in a file. Written in C++ for learning
how storage engines work.

Planned parts:

- disk manager (reads and writes 4096 byte pages)
- buffer pool
- B+ tree
- write ahead log
